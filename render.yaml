services:
  - type: web
    name: morse-bot
    env: python
    buildCommand: pip install --no-cache-dir -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
    runtime:
      - python: 3.11.10
